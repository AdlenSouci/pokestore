# Optionnel : même base que `frontend` → `VITE_API_URL` (port 3000, préfixe /api).
# Si absent, l’app tente de déduire l’IP du PC depuis Expo (Expo Go sur le même Wi‑Fi),
# puis émulateur Android (10.0.2.2) ou simulateur iOS (127.0.0.1).

# EXPO_PUBLIC_API_URL=http://192.168.1.199:3000/api
